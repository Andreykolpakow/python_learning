Задание 1.
Сложность была только в понимании формулировки задания (разница в выражении и его значении)

Задание 2.
Ужас. Потратил на него бльше 10 часов, собрал код из говна и палок, по принципу только
make it work))) Перечитал подсказки и зеленую, и жёлтую. Не хватило красной, коричневой и чёрной)))
Сложности были 2 типов:
1) найти необходимый метод/функцию и разобраться с синтаксисом (это особенно сложно)
2) найти ошибку в коде. Например, сначала пытался собирать строку через цикл for i in range(len(rezult_list)),
но оказалось, что он на каждой новой итерации игнорирует i += 3 из кода и прибавляет 1 к значению i из прошлой.
Потому цифры выводились два раза.

Задание 3.
Сделал быстро, но на усложненное время уже не тратил, т.к. и так отстал от графика.

Задание 4.
Сначала решил сделать разделение через .split, но выдавало ошибку 'float' object has no attribute 'split'.
Далеко не сразу понял, что это он не на точку ругается, а на то, что ему дали не строку, а float число)))
Пробовал и через split и rsplit, но быстро не нашел способа правильно определять 00, 05, 50 и 55 копеек.
Постоянно терялся 0 то слева, то справа.
В результате посмотрел подсказки и зеленую, и желтую, но ответа там не нашёл (
В итоге сначала price привёл к строке, и уже потом разделил.
Самые высокие цены вывел с помощью .pop. Помню, что это ресурсозатратно, но сейчас make it work